计算机网络
========
沉默王二-计算机网络
问题:
- 说下计算机网络体系结构
- 从浏览器地址栏输入 url 到显示网页的过程了解吗？
- 说说 DNS 的解析过程？
- 说说 WebSocket 与 Socket 的区别？
- 说一下你了解的端口及对应的服务？
- HTTP 8-23题
- TCP 24-63
- UDP 44-47



三个术语: OSI网络模型, TCP/IP四层模型, TCP/IP五层模型

他们之间的联系.

用一张图标识三个模型的联系:

![三分恶面渣逆袭：三种网络体系结构](https://cdn.paicoding.com/tobebetterjavaer/images/nice-article/weixin-mianznxjsjwllsewswztwxxssc-11ecdc9c-5a06-4429-bfc4-115793749000.jpg) 

应用层-表示层-会话层 是osi独特的内容,他们对应的功能是:

- 应用层: 提供了网络服务与用户应用软件之间的接口。如Web 浏览器
  - HTTP,DNS,FTP
- **表示层**:负责数据的转换、压缩和加密,确保从一个系统发送的信息可以被另一个系统的应用层读取。
  - JPEG,GIF
- **会话层**:管理用户的会话，控制网络上两节点间的对话和数据交换的管理。
  - RPC,SQL

其他osi层都能与tcp/ip和五层结构对应.

- 传输层-端到端的通信服务,保证数据的完整性和正确顺序。这一层包括 TCP 和 UDP 等。
  - TCP,UDP
- 网络层-在多个网络之间进行数据传输，确保数据能够在复杂的网络结构中找到从源到目的地的最佳路径。这层使用的是 IP协议。
  - IP,ICMP,ARP
- 数据链路层-在物理连接中提供可靠的传输，负责建立和维护两个相邻节点间的链路。包括帧同步、MAC地址
- 物理层-在物理媒介上实现原始的数据传输,如光纤



数据在各层传输方式,以及为何数据链路层要在首尾添加控制信息?

![数据在各层之间的传输](https://cdn.paicoding.com/tobebetterjavaer/images/nice-article/weixin-mianznxjsjwllsewswztwxxssc-6e4a8326-992c-442a-8265-5dc3d179b689.jpg)

数据链路层相比其他层多了一个尾部T2,主要是用来存储根据CRC(循环冗余校验)计算得到的帧校验序列,校验序列必须放在尾部.

为什么数据链路层需要CRC?

数据链路层直接面对物理传输，比特差错率相对更高，需要更强的校验机制,承担了**差错检测**的职责

应用场景: 

从输入URL到显示网页的过程,包含哪些步骤,每个步骤对应的协议

1. 首先进行DNS解析把域名解析为IP地址
2. 通过解析得到的 IP 地址与服务器建立 TCP 连接。这一步涉及到 TCP 的三次握手，用于确保双方都已经准备好进行数据传输了,建立TCP连接用到IP协议,IP数据包在路由之间选择用到OPSF协议,路由器与服务器通信会用ARP协议将IP转为MAC地址
3. 浏览器构建 HTTP 请求，包括请求行、请求头和请求体；然后将请求发送到服务器。
4. 服务器接收到 HTTP 请求后，根据请求的资源路径，经过后端处理，生成 HTTP 响应消息；响应消息包括状态行、响应头和响应体。
5. 浏览器接收到服务器返回的 HTTP 响应数据后，开始解析响应体渲染界面
6. 断开连接:四次挥手



DNS解析过程?

![三分析面渣逆袭：DNS 解析流程](https://cdn.paicoding.com/tobebetterjavaer/images/nice-article/weixin-mianznxjsjwllsewswztwxxssc-03408af8-3ca8-49bd-9244-6afa6fe132c6.jpg)

首先检查DNS缓存: 顺序是 浏览器缓存-操作系统缓存-ISP缓存,如果没有缓存:

向**根域名服务器**/发送请求,根域名请求顶级域名服务器如com,然后再请求权限域名服务器,权限域名服务器会返回ip地址.

服务器常用端口以及对应的服务

21 FTP;22 SSH;53 DNS;80 HTTP;443 HTTPS;1080 Sockets;

WebSocket 在哪一层? WebSocket与socket的区别, 与HTTP的区别

在应用层.

Socket 其实就是等于 **IP 地址 + 端口 + 协议**,是对 TCP/IP 的高度封装.方便网络编程.

WebSocket 是一个持久化的协议，它是伴随 H5 而出的协议，用来解决 **http 不支持持久化连接**的问题。

Socket 一个是**网编编程的标准接口**，而 WebSocket 则是应用层通信协议。

## HTTP/HTTPS

先搞明白报文结构,由报文结构延伸出各部分作用.报文结构?

分为请求报文和响应报文,请求报文由请求行、请求头部、空行和消息正文组成. 请求行没有ip地址,因为在发送http报文前,就已经通过ip建立了tcp连接.

```text
GET /index.html HTTP/1.1 # 包括请求方法、请求 URL 和 HTTP 协议的版本
Host: www.javabetter.cn
Accept: text/html
Content-Type: text/json
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/58.0.3029.110 Safari/537.3
# 请求头部和消息正文之间有一个空行，表示请求头部结束
```

响应报文结构:

```text
HTTP/1.0 200 OK # http版本,状态码
Content-Type: text/plain
Content-Length: 137582
Expires: Thu, 05 Dec 1997 16:00:00 GMT
Last-Modified: Wed, 5 August 1996 15:55:28 GMT
Server: Apache 0.84
<html>
  <body>沉默王二很天真</body>
</html>
```



响应报文状态码含义?

100 Continue;200 OK; 3XX代表重定向,301代表资源永久移动,302：临时性性移动，服务器从另外的地址响应资源,304 Not Modified 表示资源未修改，客户端可以使用缓存;4XX客户端错误,404 Not Found 表示资源不存在;5XX服务器内部错误,500 Internal Server Error;

各种请求方式以及之间的区别?

GET,只用于获取数据，并且是幂等;POST,可能会创建新的资源或修改现有资源;DELETE、PUT,用于替换指定的资源。如果指定的资源不存在，创建一个新资源; HEAD返回URL资源的首部,类似于 GET 请求没有响应体,用于检查资源是否存在，验证资源的更新时间等;OPTIONS：用于获取服务器支持的 HTTP 请求方法。通常用于跨域请求中的预检请求（CORS）;

GET和POST的区别?

传参方式:get通过URL传参,POST通过请求体传参;幂等性:get是幂等的,POST不是;缓存性:GET请求容易被缓存,POST请求则不会;

URL的格式与URI的区别?

URL是URI的子集,除了提供了资源的标识，还提供了资源访问的方式,URI只是资源的标识,你不一定知道如何获取资源.

HTTP的不同版本以及区别?

HTTP1.0

**HTTP1.0** 默认是短连接.这里的短针对的是TCP连接.会建立多个TCP连接,客户端的请求和响应是同步的,必须获得响应才能发送下一个请求.

HTTP1.1

默认开启长连接,引入了持久连接（也称为 HTTP keep-alive）.在一个连接上发送多个请求和响应。极大减轻了 TCP 连接的开销。

- **流水线处理**：HTTP 1.1 支持客户端在前一个请求的响应到达之前发送下一个请求，以提高传输效率。

HTTP2.0

采用的**多路复用,**一个 TCP 连接上可以同时进行多个 HTTP 请求/响应，解决了 HTTP 1.x 的队头阻塞问题。队头阻塞指的是响应必须按序返回,如果第一个响应生成很慢,那么后面的响应就会耽误. HTTP2.0使用二进制HTTP报文,把二进制报文拆成帧Frame,一次请求/响应为一个流Stream,所以一个TCP连接上同时进行多个流.

多路复用没有解决什么问题?

没有解决**TCP 层**的队头阻塞.TCP 是按序交付的，如果底层某个 TCP 包丢了，即使后面到达的数据属于不同的 HTTP 流，TCP 也必须等丢失的包重传成功后才会把数据交给上层。

使用二进制而不是文本格式来传输数据.

**头部压缩**：HTTP 协议不带状态，所以每次请求都必须附上所有信息。HTTP 2.0 引入了头部压缩机制，可以使用 gzip 或 compress 压缩后再发送，减少了冗余头部信息的带宽消耗。

服务器可以主动向客户端推送资源，而不需要客户端明确请求。

HTTP3.0

基于 QUIC 协议,Quick UDP Connections,解决TCP队头阻塞,在传输的过程中就完成了 TLS 加密握手



HTTP 性质: 无状态的, 具有缓存技术

场景: 

HTTP下载文件块,用多线程下载文件块.通过 HEAD 请求获取文件的总大小。然后根据文件大小和线程数，将文件进行切割。每个线程负责下载一个特定范围的数据。设置 HTTP 请求头的 Range 字段指定下载的字节区间.

对Session 和 Cookie的实现, 他们的联系和区别?

Cookie 是保存在客户端的一小块文本串的数据,Session 指的就是服务器和客户端一次会话的过程.cookie 保存在客户端浏览器中，而 session 保存在服务器上. 服务端可以根据这个 Cookie 判断用户的身份和状态,而客户端浏览器再次访问时,用session判断用户的状态.

区别: 保存位置不一样, 文件格式不一样:Cookie 只能保存 ASCII,Session保存任何类型,如userId. 保存时间不一样,Cookie长时间保存,session会在用户注销和超时后失效;文件大小限制:Cookie不能超过4k,session无限制.

他们的联系: 建立session时,SessionID传递给客户端,浏览器把SessionID保存到cookie,第二次请求时把cookie发给服务器,就可以得到sessionId,服务器判断已经登录.

如果客户端禁用Cookie,如何保存和传递sessionId? 

SessionID 的存储，可以使用客户端的本地存储,传递方式:拼接到 URL 里：直接把 SessionID 作为 URL 的请求参数;放到请求头里：把 SessionID 放到请求的 Header 里，比较常用。

JWT是什么,如何支持?

json web token, session的实现是让服务端保存状态,而jwt是服务端无状态的. 用户登录后服务器根据用户信息生成token,返回给用户,之后的请求中用户带上token,服务器端再验证并提取信息. 所以用户信息token保存在客户端.

WT 天然无状态，任何一台服务器都能独立验证，非常适合微服务和分布式架构。

跨域方面，Cookie 受同源策略和域名限制，跨域需要额外配置。JWT 放在请求头里，不受这些限制

但是jwt没法主动注销,一旦签发，在过期前服务端无法主动让它失效,用到双token策略.

组成部分, Header.Payload.Signature

JWT一定配合HTTPS才能使用,因为他是无状态的.

Authorization: Bearer <token> 是什么意思?

Bearer 是一种认证方案,为了让服务器知道客户端用的是哪种认证方案，需要一个前缀来区分。常见的有：`Basic`（用户名密码 Base64 编码）、`Bearer`（持有令牌即可）、`Digest`（摘要认证）等

HTTP与HTTPS的区别,HTTPS连接建立的过程,HTTPS客户端如何校验证书

最好用图来表示, 能够回想出来即可.

## TCP/UDP

建立连接的过程-三次握手

对三次握手的仔细探究:

1. 为什么需要三次
2. 三次握手中某一次没收到报文的情况处理
3. 第三次握手可以携带数据吗
4. 半连接状态

**ACK 的含义是"我期望收到的下一个字节"**。

断开连接的过程-四次挥手

仔细探究:

1. 为什么需要四次挥手
2. 为什么等待2MSL
   1. **为了保证客户端发送的最后一个 ACK 报文段能够到达服务端。**服务端会超时重传这个 FIN+ACK 报文段，而客户端就能在 2MSL 时间内（**超时 + 1MSL 传输**）收到这个重传的 FIN+ACK 报文段。接着客户端重传一次确认，重新启动 2MSL 计时器。最后，客户端和服务器都正常进入到 **CLOSED** 状态。
   2. MSL 是 Maximum Segment Lifetime，报⽂最⼤⽣存时间，它是任何报⽂在⽹络上存在的最⻓时间，超过这个时间报⽂将被丢弃。所以⼀来⼀回需要等待 **2** 倍的时间。

3. CLOSE-WAIT 和 TIME-WAIT 的状态和意义
   1. close-wait是服务端.服务端收到客户端关闭连接的请求并确认之后，就会进入 CLOSE-WAIT 状态.而 CLOSE-WAIT 状态就是为了保证服务端在关闭连接之前将待发送的数据处理完。
   2. 在 TIME_WAIT 状态中，客户端可以重新发送 ACK 确保对方正常关闭连接。在 TIME_WAIT 持续的 2MSL 时间后，确保旧数据包完全消失，避免它们干扰未来建立的新连接。

4. TIME-WAIT状态过多



建立连接之后,发送TCP报文

TCP报文的格式:

- 源端口号和目标端口号
- 序号seq,确认号
- ACK,SYN,FIN
- 窗口大小,校验和.

TCP保证数据可靠传输的方式:

1. 三次握手保证连接可靠
2. 校验和
3. 序列号/确认应答
   1. 每个报文都有序列号,发送方发送之后都要收到服务器的确认应答ack报文

4. 超时重传
   1. 重传机制.如果发送方发送的数据包超过了最大生存时间，接收方还没有收到，发送方会重传数据包以保证丢失数据重新传输。
5. 流量控制
   1. 接收方会发送窗口大小告诉发送方它的接收能力。发送方会根据窗口大小调整发送速度，避免网络拥塞。
   2. 滑动窗口来控制. 
      1. 建立连接之后,初始化双方窗口大小

6. 拥塞控制
   1. TCP 会采用慢启动的策略
   1. 四个阶段,慢启动,拥塞避免,拥塞发生,超时重传

TCP与UDP的区别

DNS 为什么要用 UDP?

TCP 是面向字节流的协议，UDP 是面向报文的协议？这里的「面向字节流」和「面向报文」该如何理解

## IP

ARP协议的工作过程

IP与MAC地址区别

ICMP与PING的关系