派聪明学习
=============

https://paicoding.com/column/10/13

派聪明面试题： https://acnirqxk7keh.feishu.cn/wiki/HDxpwYPR4ioiieka7Olc0o8NnHd



首先讲下派聪明的技术栈,springboot+maven+mysql+spring data jpa,用redis做缓存,ES搜索引擎,kafka做消息队列. 安全认证是spring security和jwt,实时通信用websocket

MinIO + Apache Tika是文件存储和解析,ai集成用deepseek和qwen embedding,还用到了响应编程

通过 Kafka 实现异步任务调度，使用 Redis 缓存优化对话上下文管理，基于 MinIO 实现大文件分片上传与断点续传。集成 Elasticsearch，实现「关键词+语义」的双引擎搜索

基于 JWT+Spring Security 实现的多租户认证授权体系。

项目采用了 Mockito 注解驱动的测试模式，践行测试驱动开发（TDD）的理念



这个项目作为rag项目,要了解有哪些业务以及对应的实现方式:

1. 上传文件,支持大文件的断点续传和分片上传,
   1. 分片状态使用 Redis 的 BitMap 进行保存
   2. 文件本身通过 MinIO 进行存储
2. 对文档提取文本信息,然后将长文档智能切分成多个语义完整的文本块。
   1. apache Tika从文档中提取纯文本
3. 把文本转为向量,把向量伴随着原始文本一起存入到ES中,构建知识库索引
   1. qwen embedding负责转向量,ES负责存储混合索引
4. 用户提出问题,项目进行混合检索,并根据用户的组织权限，自动过滤出用户有权访问的文档内容。
   1. 通过ES进行混合检索,进行语义向量搜索以及关键词精排
   2. 使用spring security进行权限过滤,
5. 得到信息后作为上下文,连同用户的问题发给deepseek,然后把deepseek的响应进行流式输出
   1. DeepSeek 会基于我们封装好的 Prompt，生成准确、相关的回答。
   2. websocket提供流式响应支持

可以看出,涉及到大模型应用开发,关键技术在于: RAG,文档上传和解析,集成 DeepSeek 和Embedding, 这里的技术都有对应的问题与解决方案.



但一个项目最开始都是需求分析,需求跟业务直接绑定,所以需求分析与技术方案很重要.

# 需求分析

派聪明是什么?

解决了哪些痛点?

- 解决信息孤岛问题,文档散落在不同位置,无法聚合.提供统一的文件管理,支持各种文档格式,并且用Apache Tika 提取文本内容

- 引入组织标签,不同部门的文档互相隔离,保证了安全和权限隔离,
- 大文件上传,使用分片上传技术,大文件切成小块
- 使用混合检索策略避免关键词检索查询出的内容不全不相关的问题
- 集成rag技术,根据用户的prompt,检索知识库中的文档,基于文档作为上下文发给大模型,实现增强生成.
- 用websocket建立长连接,流式响应

要知道有哪些模块,以及为什么这么分块?

首先安全和权限问题是底线;对文档的提取和转为向量;问多个问题时记忆上下文;文档越来越多快速找到想要的;把派聪明问答能力嵌入到现有系统中

对应的模块是:

- 安全和权限: 用户管理模块.
- 对文档的上传,提取和转为向量: 文档上传与解析模块
- 用户提问后系统需要精准检索,这就是检索模块, (文档越来越多快速找到想要的) 要有权限控制
- 聊天助手模块,让人际交流变得流畅; 聊天记录模块记忆之前的聊天.如何保存和利用对话成果
- 文档管理与组织模块,需要更完善的文档组织功能，如分类目录、标签系统、权限管理等

项目的性质有: 文件上传和解析, 权限管理,动态知识库更新,聊天助手,多轮对话,混合检索,RAG

# 技术分析



首先根据需求拆分对应的技术方案.

![派聪明整体设计方案](https://cdn.tobebetterjavaer.com/paicoding/2c734062025198919a138d6227b102ce.jpeg)



![img](https://cdn.tobebetterjavaer.com/paicoding/c23df394db15a018c2216c71bc028285.png)





jwt+Spring security的实现,涉及到短期 Access Token + 长期 Refresh Token 的双 Token 策略



向量化处理在kafka消费者完成,作为一个异步任务.	

你印象最深刻的 bug ?

# 用户管理模块



跟用户管理模块直接相关的有:

1. controller: user和admin
2. filter: jwt和orgtag
3. Spring security,根据role配置页面访问权限,并添加filter

定义JwtAuthenticationFilter和OrgTagAuthorizationFilter

JwtAuthenticationFilter: 解析请求头的jwt token,然后验证token是否有效,采用双token验证. token有效,把根据token的信息,根据用户名查询数据库用户数据,然后创建UsernamePasswordAuthenticationToken对象,导入SecurityContextHolder.Spring security就可以获得用户身份.

熟悉jwtutils, jwt的claim保存什么? 保存tokenid,userid,role,组织标签以及主标签. jwtutils在生成token和refresh token时,保存token id和对应的信息到redis中,之后验证token是否过期直接使用redis.

SecurityConfig 是Spring security的配置类,配置类唯一的工作就是配置securityFilterChain 的bean,

在项目中,根据身份信息配置页面访问权限.并且在这个bean里面,添加JWT认证过滤器在UsernamePasswordAuthenticationFilter前面,组织标签过滤器orgTagAuthorizationFilter应该在JWT过滤器后面.



orgTagAuthorizationFilter用于实现基于组织标签的数据访问控制,组织标签的目标就是数据访问权限控制.管理员可以创建,修改,删除组织标签,可以给用户赋予标签.

在文件上传时,获取用户的主组织标签,进行自动关联.

需要解决的问题:

- 对特定资源的访问需验证用户是否有权限
- 某些API只需验证用户身份并传递用户ID如上传文件、获取文档列表等接口,不涉及特定资源



用户管理模块实现了什么功能?目标是什么?

围绕身份验证,角色管理,基于标签的数据权限隔离. 目标是实现用户注册登录,根据用户的角色权限进行接口权限隔离,只有管理员才有钱访问管理员相关接口,

根据用户的标签判断用户是否有权访问对应的数据,实现**多租户数据隔离**,在用户进行数据检索和访问文件内容时,基于组织标签进行过滤,确保用户只能访问自己组织的资源和公开的资源.

为什么要引入标签?

RBAC的权限粒度不够细,并且没有办法基于数据做动态授权, 在多部门多租户的企业环境下,RBAC容易出现角色数量爆炸问题,并且无法基于文档的属性去判断用户是否有钱访问. 所以标签权限管理本质上是attribute based,系统动态的根据用户标签以及数据标签进行匹配,实现更细粒度以及动态授权.



为什么选择jwt token方案?

jwt是无状态的,并且包含具体数据. 不需要进行session同步,并且可以直接获取token包含的信息,以减少数据库查询

如何设计payload信息? 放这些信息有什么好处和风险?

放了用户的 userId、**角色**、组织标签**以及**主组织标签这四类核心信息。并且在生成jwt token时,将这些信息保存到redis中,

组织标签多,orgTags 字段可能会变得很长.

jwt请求过来后,后端如何处理?

请求->JwtAuthenticationFilter处理->提取jwt->签名校验,过期检查->解析出用户信息->查询数据库获取完整信息->封装成UserDetails对象->封装到UsernamePasswordAuthenticationToken->导入SecurityContextHolder

OrgTagAuthorizationFilter实现组织标签授权,用户登录后访问特定的组织标签资源,处理流程?

JwtAuthenticationFilter把用户信息注入Spring Security 的安全上下文后->根据 SecurityConfig **中配置的接口访问规则进行角色级别的**基础权限校验->执行controller前,进行OrgTagAuthorizationFilter->查询数据库,保存用户的组织标签到请求上下文->请求最终被路由到搜索模块HybridSearchService，系统会构建一条带有权限过滤条件的 Elasticsearch 查询->查询加入强制过滤,只返回公开文档以及用户组织文档

用户有多个组织标签会不会权限冲突?

通过 OrgTagAuthorizationFilter ,当一个用户拥有多个组织标签时,系统会处理权限冲突:

只要用户有该资源的任何一个标签,就获得访问权限;公开资源都可访问,私有资源只有资源所有者可以访问,管理员访问不受限制.

组织合并如何处理?

给新组织设置新标签,把新标签发送给原来的两个组织的所有人和所有资源.然后逐步删除旧标签.系统保证用户标签变化后即时生效:在用户便签改变后,清理原有标签,设置redis中为新标签

组织标签树的设计考虑了哪些因素？在处理多级组织结构时遇到了哪些挑战，如何解决？

给用户和资源都打上“标签”，但标签是可以层级化的。比如 “总公司/事业部/研发组” 这种,用户访问资源时,把用户的标签向上汇总,计算用户的完整标签组.存储到jwt和redis中. 

挑战:多层组织结构意味着，判断用户是否有权限访问某个资源，理论上要从叶子节点一路向上查到根节点，非常耗时，尤其是在高并发场景下,我们会在第一次访问资源时,把所有的权限标签计算出来,保存到redis中,后续请求可以直接从redis取.



jwt是如何设计的?

签名密钥放在配置文件中,用**HS256 算法**对token计算签名,token只放必要信息. 使用双token,**Access Token 的有效期为 1 个小时**,refresh token的有效期为7天,保证用户体验降低token泄露风险.

将 access token、refresh token 存入 Redis，Redis 的过期时间比 JWT 多 5 分钟缓冲，避免 Redis 提前过期导致验证失败。

JWT 的权限拦截器会对接下来的每一次请求进行 token 验证，这里我们做了双重验证。第一重校验 Redis 缓存中 token 是否在黑名单，是否有效；第二重校验 JWT 中的 token 签名是否正确；验证成功后，再从 Token 中解析出用户信息，并设置到 Spring Security 的上下文中，以便后续的授权操作。

token 的刷新是无感知的自动刷新，在 token 验证成功后，我们会检查 token 的剩余有效期，如果少于 5 分钟，系统会自动进行 token 刷新，生成一个新的 access token；并将新的 token 返回给前端。



## 文件上传与解析

通过minio和redis保证大文件上传的可靠性.

kafka异步处理文档的解析,并提取文本用于后续向量化

所以涉及到: 分片上传与断点续传,文档合并,文档文本提取与向量化 .其中使用kafka进行异步处理,实现对文档的管理.

流程:

客户端计算md5,发起上传请求→服务端验证文件是否已存在，返回分片策略

客户端根据策略上传文件,服务器保存分片文件到minio,redis存储分片上传状态.分片上传完成后触发合并. 合并之后发送kafka任务提取文本,提取完毕后发送kafka任务进行文本转向量,最后把向量数据写入到es

Controller分别对应uploadController,parseController,DocumentController,分别对应上传文件(分片上传和断点续传),解析文件(),用户对文档操作(获取用户文档列表,下载文档,预览文档,删除文档)

对于service: 

1. UploadService,FileTypeValidationService对应上传文件时调用的服务.
2. 有 parseService和vectorizationService对于kafka消费者中进行的操作.
3. DocumentService



分块merge之后,分块在minio中删除,会在数据库也删除吗? 看uploadservice,只删除了redis的分片记录和minio的临时分片,没有删除mysql中chunk_info表中的分片.

kafka的消费者对应FileProcessingConsumer.

文本转向量用到EmbeddingClient



Apache Tika文档解析是怎么做的?




## embedding 相关


qwen embedding的原理是什么?

## 大模型Deepseek 相关
DeepSeek大模型怎么集成做生成?

## WebSocket怎么流式输出?



# 问题

## 用户管理

多租户RBAC权限体系的设计思路。

## 文件上传问题

大文件分片上传和断点续传的实现（Redis BitMap管理分片状态）

进行性能优化

## 解析与向量化问题

Kafka在项目里怎么做异步任务调度

文件合并完毕后，发送kafka消息，消息中包含文件md5，minio地址，权限信息等，定义kafka消费者后台监听，实现异步调度，消费者会对上传的文档进行文本解析以及将文本向量化，并将向量数据存储到es和mysql中。

文档上传之后怎么通过Apache Tika解析文本？

创建一个流式处理器，Tika读取文件输入流，然后在流式处理的过程，如果积累的文本达到了父块大小，则触发processParentChunk，把父块切割成子块，保存到数据库。

- 为啥要有父子文档，父文档默认大小1MB，是tika解析中的缓冲区边界。父文档是中间产物，处理完清空。
- 子文档远小于父文档，就是chunk，由父文档按语义边界切分。先按段落 → 再按句子 → 最后用 HanLP 分词 → 兜底按字符
  - 使用HanLP智能分割超长句子？把超长句子按词分割，保证切分点在词语边界上，避免破坏词的意思。
- 父文档是为了避免OOM，而子文档是chunk



文本怎么切片（文本分块策略）

首先固定大小切片，出现语义截断。

然后引入语义感知，首先按照段落切割，段落太长则细化到句子级别。

刚好一个知识点在交界处截断，则引入重叠分块机制，每个分块都有部分前后上下文冗余，从而提升召回率。

切片之后怎么通过豆包Embedding转成2048维的向量

获取文档所有分块，把文本内容集合传递给embeddingClient.embed(texts)，每一个文本获得一个float[] 2048维度向量。

具体做法是，每100个文本块一批，调用一次api

调用api： 构建请求体，包含模型类型，维度，文本块，webClient.post()，用 Spring WebFlux 的 WebClient 进行发送。网络错误则重试三次。然后解析响应，或去对应的向量。



向量怎么存到Elasticsearch里

首先预定义es的Mapping，Mapping包含 文档id，文件md5，分块id，文本内容，向量内容，模型版本，用户id，组织标签，是否公开

在根据chunk text获取到对应的向量后，封装成ESDocument领域模型，与es的底层索引相对应。

把这个`List<EsDocument>`·转为bulkOperation，每一个文档对应一个Ope，然后得到`List<BulkOperation>`,这个list可以被批量操作，把多个写打包成一次网络请求发送。

## Kafka问题

什么是消息的重复消费? 

如何保证消息不丢失

保证消息重试

项目的kafka配置参数

项目如何用kafka做异步任务调度？

解耦上传与重处理链路，实现异步，避免接口阻塞和超时。

可重试和失败隔离。 以及水平拓展，任务量大可以拓展消费者实例。

## ES问题

<details>
<summary>ES的原理</summary>
ES的index对应mysql的表,index中有各种document,document有各种field
index 是 schema-free 的,向index插入时index不存在则会生成
如果字段没见过,则会自动推断类型 dynamic mapping
</details>

<details>
<summary>如何向ES插入数据</summary>
插入只需要目标index,id(可选,es可自生成),document(文档内容,类似json)
</details>

<details>
<summary>ES的原理?</summary>
ES 的核心是倒排索引（inverted index）——它在写入时就把文本拆分成词,项目中用到IK分词器,建立词->包含这个词的所有文档的映射.
</details><details>
<summary>es查询的过程</summary>
查询时你搜一个关键词，ES 通过倒排索引直接找到包含这个词的所有文档,按相关性打分排序。
</details>

<details>
<summary>ES混合检索怎么实现的?</summary>
正确答案:我不知道
</details>

## Redis问题

缓存击穿,穿透,雪崩

常用数据结构

常用命令

为什么快

项目中哪里用到redis

用到bitmap的原因：

分片状态只有0和1，用bitmap适合分片多，性能好。

##混合检索的问题

用户提问的时候怎么做混合检索（关键词加语义双引擎）

混合检索是一个接口，用户传入检索关键字和topK，系统调用hybridSearchService，执行带权限或者不带权限的搜索。

首先获取用户的标签集合，orgTagCacheService中利用redis缓存标签集合。

然后把检索关键字转为向量， 向量生成失败仅用关键字检索。

然后执行esClient.search。 首先进行KNN召回，基于topK*30的KNN窗口大规模召回。计算出对应的KNN分数。

- KNN召回的原理？
- HNSW 近似算法

然后进行过滤，使用es的must match or，过滤没有一个关键词匹配的，并且用filter过滤，保证文档用户有权限访问。（三种情况）

最后进行rescore，使用计算出BM25_rescore分数，并且使用and，控制rescore过程中KNN部分权重为0.2,关键词匹配是1.0。 然后选取topK，把es的响应转为SearchResult领域模型集合。

过滤掉topK中分数低的文档，用minScore

为什么要做混合检索而不是只用向量检索？也就是混合检索相比纯向量检索和关键字检索的局限

传统的 BM25关键字检索缺乏语义理解，纯向量检索虽然包含语义，但是体现不出关键字检索中命中关键字多的文档的优先级，并且语义检索对一些专有名词，这种精确内容不敏感，所以要结合关键字检索。

rescore所做的内容是重排序，把BM25检索中得分高的放在前面。，利用关键字检索的精度。

评估混合检索的质量

通过哪些维度评价检索的结果质量：

- TopK中真正相关的比例。
- 看看有没有漏掉关键文档，也就是召回率。

进行ab测试，收集用户反馈。

检索接口响应慢的性能优化？

检查ES的查询效率，服务端的代码逻辑。能不能加缓存。

ES调整内存，查询效率中：调整KNN召回窗口，避免太多字段参与检索

缓存中：把计算得到的用户标签集合存到redis中，针对热点查询缓存向量内容，相同 query 不重复调用 API

## 结合大模型的问题

检索结果怎么拼装成Prompt以送给DeepSeek生成回答

首先获取聊天框的上下文内容，获取对话历史，然后调用混合检索接口，获取到`List<SearchResult>`,把搜索结果转为格式：`[1] (test1.txt | MD5:abc123def456) 内容`的格式。



涉及到deepSeekClient.streamResponse，首先根据内置prompt，上下文历史，和搜索结果构建发送给大模型的prompt，系统检索的内容和内置prompt为system级别，而用户的问题为user级别，避免user的问题影响。

## WebSocket流式输出的问题

回答怎么通过流式输出推送给前端

客户端发送WebSocket建立请求，与后端建立WebSocket连接，建立连接后后端调用chatHandler.processMessage处理普通消息。做到结合大模型回答后，

使用webflux，请求体为构建的prompt。使用.subscribe，每当deepseek响应一个chunk后，调用subscribe回调函数，提取出内容，调用session.sendMessage，把提取出的内容格式化之后，利用WebSocket发送一个格式化文本消息给前端。

## AI RAG问题

RAG和大模型微调的区别是什么，各自适用什么场景？

RAG适合知识会频繁更新的场景，微调适合需要让模型学会特定风格或能力的场景，RAG不需要重新训练模型成本更低。

Embedding是什么、为什么能捕捉语义信息？

向量化指的是把文本转为固定长度的浮点数组，使得语义相近的文本，向量也相近。

1. 模型在海量文本上预训练，学会了把文字映射到一个高维空间--transformer编码器。
2. 语义压缩：输入的文本在模型内部进行多层注意力运算，最终输出固定维度的向量，浓缩语义信息。

维度越高，就更能区分细微的语义差异。



向量检索的基本原理。ANN近似最近邻搜索是怎么加速检索的（了解HNSW的大致思路就行，不需要推公式）。

RAG效果不好怎么优化？

切片策略，加入上下文重叠保证连贯性。

选择维度更高的embedding模型，或者换模型

检索阶段加入re_rank

Prompt设计优化

大模型幻觉是什么，RAG怎么缓解幻觉。

大模型会编造不存在的信息，RAG通过让模型基于检索到的真实文档来生成回答，减少了凭空编造的可能性。

rag的幻觉是什么？

RAG 中的"幻觉"是指大语言模型生成的内容与检索到的真实信息不符，或者模型编造了不存在的信息

要确保只有高质量的上下文才被传递给大模型，给每个上下文添加来源标识。



设计置信度评分机制来判断检索结果的可靠性？

置信度的关键维度： 向量相似度 KNN计算计算余弦相似度。

关键字匹配度：BM25打分 按权重得到最终分数



rag系统的瓶颈在哪？

在向量检索，如果文档数量多，实时的相似度计算成为瓶颈，所以使用更高效的向量索引算法 HNSW。

每个query都生成向量，导致出现延迟

大模型的推理时间，如果不用流式输出，用户会等很久。



多轮对话的上下文问题：

采用了基于 Redis 的对话历史管理机制，每个用户都有一个唯一的会话 ID，所有的对话内容都按照时间顺序存在 Redis 中，并设置了 7 天的过期时间，超过20条截断。

**金融领域**应用rag的问题：

数据的实时性和准确性要求，对于价格，汇率等高频变化的数据，标明时间戳，提醒用户时效性。

restful如何支持rag？

一切以资源为中心，把系统的核心功能抽象为资源，如混合检索，文档，对话。统一响应体。

A/B测试来优化RAG效果？

分为三个核心部分： “测什么”、“怎么评”和“如何做” 。当一个请求进来时，系统会根据用户 ID 或会话 ID，通过哈希等方式，将用户稳定地分配到 A 组或 B 组。

rag的发展预测：

首先向多模态rag发展，任何信息都可以被索引和检索。未来的rag向agent智能体发展，当它需要更精确的信息时，主动向用户追问或者调用function call外部查询。

对AIGC，RAG，AGENT的理解

AIGC，全称为 AI Generated Content，意为“人工智能生成内容”，RAG，是一种将信息检索（IR） 与大型语言模型（LLM） 的文本生成能力相结合的技术

Agent，也就是“智能体”，**一个能够感知环境、自主决策并采取行动以实现特定目标的实体或系统**，需要自助处理需要处理模型和外界的信息交互。Agent 最大的特点是，借助 **Function Call 模型**，可以自主决策使用外接的一些工具来完成特定的任务



什么是MCP？

模型上下文协议，解决LLM与外部工具的集成问题

rerank中的RRF和大模型重排

RRF融合排序在混合检索中是什么？

RRF只有一个K参数，它不看分数，只看排名，比如KNN是0-1，BM25是0-x，那么如果不确定权重。用RRF绕开，只看排名。RRF不是最优解，但是不会太差，表现稳定。

rescore需要调参，在项目中就是BM25更重要，所以使用rescore分配权重。

大模型重排指的是将召回的 Top N 个文档块的内容，连同原始查询一起，通过一个精心设计的提示词全部提交给 LLM，让LLM决策排序结果。

混合检索的准确性和召回率指的是什么？

**准确率（Precision）：** 检索回来的结果里，有多少是真正相关的

**召回率（Recall）：** 所有相关的内容里，有多少被成功检索回来了

RAG检索到的知识与LLM预训练知识冲突，如何处理？

改提示词，让检索到的内容优先级大于模型内容，无法确定时说存在信息冲突。

# Spring知识

**@RequestParam** — 从**HTTP请求参数**中取值，也就是URL查询字符串或表单提交的数据。

**@RequestAttribute** — 从**request作用域的属性**中取值，这些属性是在服务器端通过 `request.setAttribute()` 设置的，通常由拦截器、过滤器或转发设置。

`@RequestHeader` 用于获取HTTP请求头中的值。把获取到的值传递给controller中的入参,@RequestHeader(value = "X-Token", required = false) String token. 实际开发中最常见的场景就是取Authorization头里的token

一个`controller`,可以返回业务对象直接转为json,也可以返回ResponseEntity<?>来自己控制响应头和响应体. 这里的? 就是响应体的类. 直接返回业务对象如果出现异常,用全局AOP来捕捉controller抛出的异常,让Controller只关心正常流程:

```java
// 全局异常处理器统一捕获，统一返回格式
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<ErrorResult> handleBusiness(BusinessException e) {
        return ResponseEntity
            .status(e.getCode())
            .body(new ErrorResult(e.getCode(), e.getMessage()));
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<ErrorResult> handleValidation(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldErrors().stream()
            .map(f -> f.getField() + ": " + f.getDefaultMessage())
            .collect(Collectors.joining(", "));
        return ResponseEntity.badRequest().body(new ErrorResult(400, msg));
    }

    // 兜底：未知异常
    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorResult> handleAll(Exception e) {
        return ResponseEntity
            .status(500)
            .body(new ErrorResult(500, "服务器内部错误"));
    }
}
```

**@RestControllerAdvice** 本质是 `@ControllerAdvice` + `@ResponseBody`。`@ControllerAdvice` 的意思是"对所有Controller生效的增强"，Spring扫描到这个注解后，会把这个类注册为全局的Controller切面，拦截所有Controller抛出的异常。

**@ExceptionHandler(XxxException.class)** 标记在方法上，声明"这个方法负责处理这种类型的异常"。Spring捕获到Controller抛出的异常后，就会找匹配的handler方法来处理。

**匹配顺序的核心规则是：精确匹配优先，找不到再去找父类,由子类往父类找,最后Exception.class兜底**

**如果Controller类内部也定义了 `@ExceptionHandler`，它的优先级高于全局的 `@RestControllerAdvice`**。也就是局部优先于全局。



实现了CommandLineRunner接口的component,在Spring启动后执行逻辑,通过order设置Initializer之间的优先级.

项目开始后进行:

1. 创建管理员账号
2. 自动创建默认组织标签以及管理员组织标签
3. 初始化es的index

```java
@Component
//没有order,是最后顺序
@Order(2) // 设置优先级，确保在管理员账号初始化器之后运行
public class OrgTagInitializer implements CommandLineRunner
```

### springMVC

WebMvcConfigurer工作在 **Spring MVC 层**,请求已经通过了 Security 过滤器之后。它负责 MVC 框架本身的行为配置——跨域（CORS）、拦截器注册、静态资源映射、消息转换器等。

客户端请求 → Servlet Filter（Security在这里）→ DispatcherServlet → HandlerInterceptor（在这里）→ Controller

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
```

日志拦截器

```java
public class LoggingInterceptor implements HandlerInterceptor
```

Spring MVC 层的拦截器，提供三个切入点：

- `preHandle` — Controller 方法执行前（可以决定是否继续）
- `postHandle` — Controller 执行后、视图渲染前
- `afterCompletion` — 整个请求完成后（适合做日志记录、资源清理）

在springMVC中,dispatchServlet请求handlerMapping, HandlerMapping 做的事情很简单：**根据请求 URL 找到对应的 Handler**,通常是controller的其中一个方法.它返回HandlerExecutionChain,这个执行链包含handler以及匹配到的HandlerInterceptor. 

DispatcherServlet 拿到这个 chain 后，先依次执行所有 Interceptor 的 `preHandle` 然后通过 **HandlerAdapter** 真正调用 Controller 方法,返回后执行 `postHandle` 和 `afterCompletion`.

所以定义HandlerInterceptor如LoggingInterceptor,然后加入WebMvcConfigurer中,HandlerMapping会找到WebMvcConfigurer匹配到的handler,把拦截器和handler打包到执行链.

Interceptor 的 `preHandle` 能拿到 Handler 信息,因为Interceptor 是 chain 的一部分，自然能拿到同一个 chain 里的 Handler 对象。



model与entity的区别:

entity/ — JPA 实体，对应 MySQL 表. model 里的类都有 @Entity，由 Hibernate 管理，增删改查都通过 Repository 操作数据库。

model/ — 普通 POJO，对应 Elasticsearch 文档或业务数据传输

entity一般对应数据库实体,而model对应业务流转数据对象.



### Filter讲解

```java
abstract class OncePerRequestFilter extends GenericFilterBean
```

Filter是Servlet规范里的概念，比Spring MVC还早。它在请求到达DispatcherServlet之前拦截，也在响应返回客户端之前拦截，形成一个链式结构

```text
客户端请求
  → Filter1 → Filter2 → Filter3
    → DispatcherServlet
      → Interceptor → Controller
    ← Controller返回
  ← Filter3 ← Filter2 ← Filter1
客户端收到响应
```

从上面看出,filter是栈调用,filter1第一个处理,则Controller返回后最后一个处理. Interceptor在DispatcherServlet分发请求后处理,在controller执行前.

重写doFilter方法实际执行业务逻辑,这里的FilterChain chain就是filter调用链.

```java
public void doFilter(ServletRequest request, ServletResponse response, 
                         FilterChain chain) throws IOException, ServletException {
```

`OncePerRequestFilter` 是Spring提供的抽象类,一次请求可能经过同一个Filter多次。比如请求被forward转发时，会重新走一遍Filter链。这就导致你的JWT解析逻辑执行了两次,所以用OncePerRequestFilter保障同一个请求只执行一次.

### Spring Security概念

Spring Security本身就是一组Filter,UsernamePasswordAuthenticationFilter是默认就在链里的.Spring Security自动配置时会把这些内置Filter按固定顺序注册到链里。`UsernamePasswordAuthenticationFilter` 负责处理表单登录（拦截POST `/login`，取username和password参数做认证）。即使你不用表单登录，它仍然作为一个位置标记存在于链中。

```text
Servlet容器的Filter链
  → DelegatingFilterProxy（桥梁，连接Servlet和Spring）
    → FilterChainProxy（Spring Security的入口）
      → SecurityContextPersistenceFilter
      → CorsFilter
      → CsrfFilter
      → LogoutFilter
      → UsernamePasswordAuthenticationFilter
      → ...你的JwtFilter...
      → ExceptionTranslationFilter
      → AuthorizationFilter
        → DispatcherServlet → Controller
```

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
```

这个安全配置类内部返回SecurityFilterChain,最终产出的 `SecurityFilterChain` 对象就是 `FilterChainProxy` 内部持有的那条链

`SecurityFilterChain` Bean定义了安全规则 → Spring把它注册到 `FilterChainProxy` → `FilterChainProxy` 通过 `DelegatingFilterProxy` 挂到Servlet容器的Filter链上 → 每个请求进来都会走这条Security Filter链 →JWT Filter和orgTag Filter在这条链里完成认证。



### 实现websocket能力 TODO

需要设置websocketConfig以及ChatWebSocketHandler类.

 @Slf4j 是 Lombok 提供的注解，自动生成一个日志对象，等于手写` private static final Logger log = LoggerFactory.getLogger(FileProcessingConsumer.class); ` 



# 技术栈 Kafka



kafka的概念

producer->broker->topic->partition->Replica

同一个topic的partition内的数据是总数据的子集,而partition的Replica是为了数据备份,分为leader和follower,存储的数据相同.

不同的consumer group互不影响,各自独立消费全量数据——这就是 Kafka 既能做消息队列（一个 Group 内竞争消费）又能做发布订阅（多个 Group 各自消费）的原因。

在一个consumer group内部,有不同consumer,一个consumer对应一个或者多个partition,但不会出现一个partition对多个consumer的情况.如果 Consumer 数量超过 Partition 数量，多出来的 Consumer 会闲置。

为什么要分区？一是并行——多个消费者可以同时读不同分区；二是扩展——分区可以分布在不同的 Broker（服务器）上，突破单机瓶颈。



Producer 决定消息发到哪个partition,一般是通过key的hash或者指定partition.

Broker 收到消息后，追加写入对应 Partition 的日志文件（append-only log）。注意是顺序写磁盘，不是随机写，所以速度非常快。每条消息被分配一个递增的 offset。

写完之后根据 `acks` 配置决定什么时候给 Producer 返回确认：`acks=0` 不等确认直接返回，最快但可能丢消息；`acks=1` Leader 写完就返回；`acks=all` 要等所有 ISR（In-Sync Replicas）副本都写完才返回，最安全但延迟最高。

副本同步: Partition 有一个 Leader 和多个 Follower。所有读写都走 Leader，Follower 主动从 Leader 拉取数据保持同步。如果 Leader 挂了，ISR 里的 Follower 会被选举为新 Leader，保证可用性。

消费者是 pull 模式——Consumer 主动向 Broker 发起拉取请求,通过 offset 去控制消费进度,随时可以进行数据回溯.

Consumer 处理完消息后，需要提交 offset 告诉 Kafka"我已经消费到这个位置了"。提交方式有两种：自动提交（定时提交，简单但可能重复消费）和手动提交（处理完再提交，更精确但需要自己管理）。

Consumer业务处理的最小单位是record,网络传输的单位是 batch,是一组record

offset 提交的粒度也是基于 partition 中的位置，不是基于单条消息的"已消费/未消费"状态,提交offset=5,意味着5之前的都消费过了.



如何确保kafka消息可靠传递?

**生产者这块**，我们设置了 acks=all，也就是生产者发送一条消息，必须等到所有同步副本都写成功，才算这条消息真正发出

把 topic 的副本数设成了 3，也就是一个 leader + 两个 follower，再加上配置了 `min.insync.replicas=2`，意思是如果只有一个副本存活了（比如只有 leader 没挂），Broker 就会拒绝写入请求。这个机制相当于在服务端也加了一道“健康检查”，防止我们写入到一个马上就挂掉的分区上，避免数据丢失。

**消费者**:没有用 Kafka 默认的自动提交，而是设置了 `enable.auto.commit=false` 转为手动提交。这样能确保只有当我们的业务逻辑真正执行成功，比如向量写入 ES 成功，数据库状态更新成功，才会调用 `commitSync()` 提交 offset。这样即使系统崩溃，重启后还能从失败点重新拉取任务，保证至少处理一次，不会丢任务。



`factory.setTransactionIdPrefix("file-upload-tx-");` kafka的生产者工厂设置事务前缀,为producer提供事务能力,调用kafkaTemplate.executeInTransaction()把多条消息放入一个事务.





保证生产者的重试机制,它会自动尝试重新发送，最多 3 次.`enable.idempotence: true` 的配置也能确保即使在重试过程中，消息也只会被写入一次

消费者的重试:通过 DefaultErrorHandler 实现了一套优雅的消费者重试与死信队列机制。FixedBackOff 会阻塞当前消费线程 ，等待 1 秒后进行第一次重试。如果再次失败，它会再等 1 秒，进行第二次重试，总共最多重试 4 次。如果 4 次后仍然失败， DeadLetterPublishingRecoverer 会接管这个消息，将它发送到死信队列中





# 业务测试用例

自动化测试框架中, 除了对单个接口的黑盒测试之外,还要编写业务流程测试用例,这是面试的关键,所以我要写一些业务流程用例:

面试会问,针对哪些模块设计了哪些测试用例

首先是多租户权限管理和RBAC模块,测试访问权限.

1. 登录管理员用户->创建标签->创建用户->把用户添加组->其他组成员

文件上传



# 面试准备

![img](https://cdn.paicoding.com/paicoding/2f45ce6e7a2377a85526498eca2f03e6.png)

文档上传之后怎么通过Apache Tika解析文本、文本怎么切片、切片之后怎么通过豆包Embedding转成2048维的向量、豆包Embedding向量化是什么原理，向量怎么存到Elasticsearch里、用户提问的时候怎么做混合检索（关键词加语义双引擎）、检索结果怎么拼装成Prompt送给DeepSeek生成回答、回答怎么通过流式输出推送给前端。这条主线你能用自己的话从头到尾讲清楚，面试就能应付80%的问题了。 

需要了解但不用深挖的部分：大文件分片上传和断点续传的实现（Redis BitMap管理分片状态）、Kafka在项目里怎么做异步任务调度、多租户RBAC权限体系的设计思路。这些能讲个大概就行，面试官追问到细节的时候你说这块了解过实现思路但没有深入优化也是可以的。

派聪明的教程加上配套的RAG面试题基本上够用了，不需要额外去找太多资料。高频的问题。 第一，RAG和大模型微调的区别是什么，各自适用什么场景。简单来说RAG适合知识会频繁更新的场景，微调适合需要让模型学会特定风格或能力的场景，RAG不需要重新训练模型成本更低。 第二，向量检索的基本原理。Embedding是什么、为什么能捕捉语义信息、ANN近似最近邻搜索是怎么加速检索的（了解HNSW的大致思路就行，不需要推公式）。 第三，为什么要做混合检索而不是只用向量检索。纯向量检索可能丢掉精确匹配的关键词，纯关键词检索又捕捉不了语义，所以两者结合再通过RRF融合排序得到最优结果。 第四，RAG效果不好怎么优化。可以从切片策略（加overlap保证上下文连贯）、Embedding模型选择、检索阶段加Re-rank重排序、Prompt设计优化这几个角度来答。 第五，大模型幻觉是什么，RAG怎么缓解幻觉。大模型会编造不存在的信息，RAG通过让模型基于检索到的真实文档来生成回答，减少了凭空编造的可能性。





简历写法：

**技术栈**：Spring Boot、MySQL、Redis、Elasticsearch、Kafka、MinIO、WebSocket **项目描述**：面向企业内部文档检索与知识问答场景，基于 RAG 架构搭建智能问答助手，支持多格式文档处理、混合语义检索与多轮流式对话。

•**分片上传**：基于 MinIO + Redis BitMap 实现分片上传与断点续传，将 1GB 文件上传耗时从 15s 优化至 3s。

•**异步流水线**：使用 Kafka 解耦上传、解析、切块、向量化流程，提升吞吐并支持失败重试与任务可观测。同时将向量和关键词存入 Elasticsearch 供混合检索。

•**混合检索**：混合检索采用向量 KNN 召回 + 关键词/BM25 二阶段重排（ES rescore）的方案，先用向量检索扩大召回窗口（recallK=topK×30），再在候选集上用 BM25（AND）精排，并保留少量向量分作为辅助，同时结合用户/组织/公开权限过滤，整体相关性与稳定性提升。

•**流式问答**：基于 WebSocket + 大模型 Stream API 实现流式输出；使用 Redis 持久化多轮对话历史，针对超长对话引入摘要记忆策略，触发大模型对早期上下文进行压缩总结，减少上下文长度带来的推理成本。

•**开发模式**：采用 JUnit + Mockito 践行 TDD，覆盖用户注册、认证、会话管理等功能的正常与异常流程；对文件分片上传、向量检索等关键环节进行压力测试与性能优化，检索响应时间从 800ms 降至 200ms。
