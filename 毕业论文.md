毕业论文
=========

## 毕业论文选题

选题确定是rag, 灵感来自于导师讨论以及派聪明rag项目.如果派聪明项目能和我的毕业论文相结合就更好了.

但是论文作为研究就要提出问题,我的这个问题可能是什么呢? 这个问题可以是

- 如何增强rag,比如加入知识图谱GraphRAG,通过结构化的关系标识来增强检索的语义深度,但是如何量化是个问题.
- 加入多模态数据库,rag一般使用向量数据库,如果我能使用多模态数据库,就能增强rag的能力,解决知识孤岛问题. 现在的派聪明项目是用rag解决查询企业级文档问题,用自然语言就能得到根据文档上下文生成的内容. 系统能够自动解析 Word、PDF、TXT 等文档,但相比于多模态数据库,支持的不够多.

我现在主要目的是写一篇硕士毕业论文,还没有选题,我想选rag+多模态数据库的题目,但是具体应用场景还不确定, 不确定论文终究要解决什么问题,或者探讨一些应用场景的对比,我现在计划在毕业后做软件测试相关工作,我手头也有一个rag项目,能够让用户与ai模型实时交流,并且应用rag减少大模型的幻觉问题.

现在根据我跟ai的对话.我总结出了一些重要内容

方向一（最推荐）：基于多模态 RAG 的企业知识库问答系统设计与实现

这个等于是我的项目+多模态能力,但是只有系统设计,没有研究问题,现有的纯文本 RAG ,意思是根据加不加图片做对比试验, 但是多模态只支持加图片是不是有点少.

""企业非结构化文档包含大量图文混合内容，纯文本 RAG 无法有效利用视觉信息，导致问答准确率不足",就要解决这个问题,然后看加上图片效果怎么样. 纯文本 RAG vs 文本+图片描述 RAG vs 多模态 RAG 对比检索召回率、回答准确率、用户满意度等指标。



方向二：面向企业知识库的混合检索策略研究与优化

聚焦在检索策略本身。问题定义为"在企业 RAG 场景下，如何优化混合检索的效果"。纯关键词检索 vs 纯向量检索 vs 不同权重配比的混合检索 vs 加 rerank 模型的混合检索,对比不同策略下的回答质量,探讨分块策略（chunk size）和 top-K 对最终效果的影响。主要工作量在实验和分析上



方向一是系统层面的"扩展多模态"，方向二是检索层面的"优化策略"，题目是《基于多模态混合检索的企业知识库智能问答系统设计与实现》

企业文档包含图文混合内容（这是问题背景）→ 你设计了一个支持多模态的 RAG 系统（这是方向一）→ 在检索环节你研究了不同混合检索策略对多模态场景的影响（这是方向二融入进来）→ 实验验证。

多模态先只做图片，不碰音视频；检索策略对比选3~4种有代表性的就够了，不要穷举；



问题: 如何对实验结果进行量化, 既然是大模型的回答,就难以量化.

回答: RAG 系统看检索指标与生成指标,

**检索阶段的指标**评估的是"找得准不准",Recall 衡量的是相关文档有没有被找回来——比如一个问题的答案散落在3个文档片段里，你的系统召回了其中2个，Recall 就是 2/3。Precision 衡量的是召回的结果里有多少是真正相关的——你返回了5个片段，其中3个真正有用，Precision 就是 3/5。需要你事先标注好每个问题对应哪些文档片段是相关的，

**生成阶段的指标**评估的是"答得好不好"。Answer Relevancy 衡量的是模型的回答是否切题——有没有答非所问。Faithfulness 衡量的是回答是否忠于检索到的参考资料——模型有没有编造参考资料里不存在的信息（即"幻觉"）。

使用另外一个大模型Ragas 进行打分.



## 论文题目

把这两个问题抽象一下，它们本质上都在回答同一个问题：**在企业知识库场景下，如何从输入端（数据模态）和检索端（检索策略）两个维度提升 RAG 系统的问答效果？**

基于这个思路，题目可以考虑：

**首选：《面向企业知识库的多模态 RAG 问答优化研究》**

这个题目简洁，"优化"是关键词，涵盖了你要做的所有事情——多模态是一种优化，检索策略调优也是一种优化，都统在"优化研究"下面。"面向企业知识库"限定了应用场景，不会显得太空泛。

**备选一：《基于多模态与混合检索策略的 RAG 问答效果优化研究》**

更具体，把两个研究维度都点出来了。好处是看题目就知道你在做什么，缺点是稍长。

**备选二：《企业知识库场景下 RAG 系统检索与生成效果的优化方法研究》**

更学术化一些，"检索与生成"对应 RAG 的两个核心环节。但"多模态"没出现在题目里，你需要在摘要里补充说明。

我倾向推荐第一个，长度适中，关键信息都有，而且"多模态"出现在题目里对当下的选题热度来说是加分项。你觉得哪个更合适？或者你希望题目里强调哪些关键词？



问题: 既然要验证混合检索,应该用什么向量数据库



问题: 多模型数据库和向量数据库的区别.



问题: 如何加入知识图谱,

答案:加入不了知识图谱了,时间不够,

问题 怎么找到企业文档,以及如何构建让Ragas 进行打分的用例.



问题:我在想只做多模态比较和混合检索策略对rag的优化会不会太少了,毕竟论文要写45-50页

https://claude.ai/share/c8c90898-1a19-479f-ac6c-da33d5c39eb1


