测试知识
=======
https://www.yuque.com/itwanger/gykdzg/mtw6mm?singleDoc#3a51cd0c
测试开发学习路线https://github.com/yangzige/qa-roadmap/tree/main
代码随想录测开突击https://www.yuque.com/chengxuyuancarl/zzsf9x/bt9uaqcakt0xb8bu
代码随想录测试面试题https://www.yuque.com/chengxuyuancarl/vpmo4o/qmd7v8tbwbpcu9ty?singleDoc#pOtI0
https://github.com/GitDzreal93/tester-interview#%E4%B8%89%E8%BD%AF%E4%BB%B6%E6%B5%8B%E8%AF%95%E5%AE%9E%E6%88%98
春招测开岗考点 https://www.nowcoder.com/discuss/353157742032527360

软件测试部落 http://www.51ste.com/front/index.php



web测试和app测试的区别?



下面是我对测试知识体系的理解,我要不断的更新以及优化结构.



测试是一个抽象的概念,根据测开的定义:利用开发能力保障质量,可以看出测试是质量保障的一部分.测试的目的就是提升软件质量,无论是接口测试,功能测试,还是性能测试,安全测试.软件的定义很广,如果一个后端系统被称为软件,那么前端和app肯定也是软件.我认为软件更像是系统,所以质量保障的对象不一定是软件,而是系统. 软件的质量保障涉及到很多方面,一个系统在多个层面都要保证质量,**所以需要了解这些方面,如兼容性**. 在了解了这些之后,就知道一个系统可能出现的软件质量问题,而一般情况下我们针对这些问题设计测试用例,自动化(利用自动化框架)执行这些测试用例,以检测质量问题. 那么我们的测试用例设计就围绕质量保障方面来设计.为了提升测试用例设计能力,需要**针对多种场景练习测试用例设计**.后端系统测试经常处理接口,而接口是系统的一部分,所以接口测试相当于对系统进行测试,也就会涉及到软件质量保障的方面.**所以针对接口测试这个特定的情况,要清楚流程是什么,如何设计测试用例**.可以看出软件质量保障的各个方面对应到测试用例时,测试用例也是针对不同的方面设计的.比如有些测试用例是功能测试的,而有些用例是性能测试的.而且每个方面的测试都有对应的流程:

- 功能测试在整个系统的开发过程中都需要进行,目的是保障系统的正确性.
- 性能测试是涉及到方方面面,为了提升系统的总体性能而设计.
- 系统的安全性依靠安全测试保障.

系统从小到大进行开发.所以测试范围也是从小到大,针对小段代码如方法,类的测试叫做单元测试,测试系统的不同模块之间的协作叫做集成测试,而针对完整的系统进行的测试是系统测试,上线到用户后用户进行验收测试.这四个测试分类是测试范围从小到大.而针对是否是直接测试系统代码还是把系统作为一个黑箱进行测试,分为白盒测试和黑盒测试,因为白盒测试测试代码,所以接近底层,黑盒测试不关心如何实现,只关心外在表现,所以层级较高,单元测试经常是白盒测试,集成测试因为要测试模块之间的交互,而模块之间的相互调用不是暴露接口,而是代码调用,所以与单元测试相似,但不关注模块的实现方式,所以属于灰盒测试.**举一个集成测试的例子**.而系统测试,因为系统已经开发完成,所以经常属于黑盒测试.用户进行体验的验收测试也必然是黑盒测试. **在系统开发中,测试应该摆在什么位置?** 刚开发的系统往往充满了bug,所以优先解决影响系统核心功能的bug,进行的测试叫做冒烟测试,而系统每次更新迭代,添加新代码后都要进行回归测试. 

在企业中,如何为保障软件质量的各个方面做出实际行动? 肯定会有对应的SOP.也就是涉及到软件测试的流程,要**清楚软件测试的流程,以及产出的文档**才能知道企业中针对提升软件质量设定了哪些过程.在流程中,如果查到了缺陷,也就是bug,从bug提出到解决,涉及到bug的生命周期,**知道bug生命周期能帮助工作**.往往通过缺陷管理工具如jira和禅道管理bug,**要熟悉缺陷管理工具的使用**. 软件测试的流程中写好测试用例后,要实际执行,而一个个执行效率低,通过自动化测试框架进行自动化测试,但也很容易想到,有些测试用例不适合自动化执行,**哪些功能适合自动化测试,哪些不适合? ** 生成测试报告是软件测试流程的一部分,测试用例执行完毕后获取测试报告,测试报告准确评估系统的质量,以及系统缺陷.

要知道如何把上面涉及到的测试方法进行落地:

- 进行白盒测试
- 黑盒测试
- 验收测试
- 功能测试
- 性能测试

我要清楚为何选择测开,**为什么选择测试?我做测试会有什么优势?对测试的理解是什么?测试与开发的区别和联系在哪? 我的职业规划是什么?**

作为一名测试开发工程师,要熟悉日常工作,**作为一个测试开发工程师，要参与哪些流程？**也会遇到日常问题,如下

- 如何判断是否是BUG?
  - 判断是否是 bug 的核心依据是需求文档和设计文档，和文档不一致的就是 bug；文档没覆盖到的看行业惯例和用户体验；有争议的拉产品经理对齐

- 与开发人员或产品经理的分歧:如果在测试中发现一个bug，但是开发经理不认为这是一个bug，应该怎么解决？
  - 告知开发bug判断依据,明确开发的理由.
  - 校验开发的理由,根据需求文档和与产品的确认.
- 如果遇到一些低概率性的bug会怎么办？
  - 记录bug情况,复现步骤,测试环境,测试数据.
  - 根据严重性,频繁性和对用户的影响来决定优先级
  - 如果产品发布日临近那么不严重不频繁的bug只需要备案

- 如何定位bug? 如何定位是客户端还是服务端
  - 首先复现bug,记录bug的复现步骤以及测试环境配置
  - 分析服务端有无异常日志,检查服务端的处理逻辑,确保请求正常处理以及正确响应.是否是服务器内部错误和响应错误?
  - 前端界面用开发者工具检查网络请求\响应数据\,响应数据有无问题?
  - 检查是否是网络问题
- 测试中重复性工作较多该怎么办?
  - 先识别 → 再评估 → 后落地;观察重复工作出现在哪些环节，然后评估哪些投入产出比最高,比如
    - 回归测试每次都手动
    - 测试用例重复性高考虑关键字驱动(**把测试操作封装成"关键字"（本质就是函数），然后用关键字的组合来描述测试步骤**。Robot Framework 就是关键字驱动的典型代表)或者数据驱动(用 YAML 文件存测试数据，然后用 `parametrize` 把数据注入到用例里，这就是典型的数据驱动。);

- 如何防止测试用例漏测?
  - 根据需求文档编写用例,每条需求都有对应用例覆盖
  - 理解业务,挖掘隐形需求
  - 多考虑异常场景和数据
  - 从多个维度进行测试
  - 站在用户的角度去思考问题,模拟用户使用场景
- 如何设计高质量测试用例?
  - 测试尽早介入,彻底理解需求;参考类似需求的测试用例.
  - 清楚输入输出的各种可能性以及关联关系,理解需求的执行逻辑
  - 找需求的特性,补充测试用例
- 如何提高测试用例覆盖率?
  - 设计方法多样化 → 用例评审 → 覆盖率度量 → 线上复盘;
    - 用多种用例设计方法以及从多维度设计用例
    - 做用例评审,不同角色看问题的角度不同。
    - **用覆盖率指标来发现盲区**。代码覆盖率（行覆盖、分支覆盖）可以用 JaCoCo 之类的工具采集，跑完自动化用例后看哪些代码分支没被执行到，针对性地补充用例。
    - **从线上 bug 反推用例遗漏**。




对上面问题的回答:

- 一个系统在多个层面都要保证质量

  - 功能(基本功能),安全(权限问题,属于功能),可维护(代码质量好),性能(性能好),可靠(正常和异常情况都可靠),兼容(能够在safari显示),易用(用户体验好,界面好用). 先写烂代码实现基本功能,实现功能时注意权限问题,然后关注代码质量,实现基本功能后测试性能,并且系统在高压等特殊情况也可靠,交付软件时兼容用户的系统,最后用户反馈好用易用.

- 针对多种场景练习测试用例设计

  - 微信朋友圈点赞;微信支付;微信红包;微信发送消息;水杯测试（一次性纸杯/普通水杯）;电梯测试用例（两台电梯联动算法）;笔;上传文件;搜索框;登录页面;洗衣机;桌子;一张白纸;椅子;ATM机;自助售货机;购物车功能;支付宝扫码;微信单人视频通话/多人视频会议;微信列表删除操作;弹幕测试;抖音上下滑动视频;测试开屏广告页面;测试后台积分管理;测试美团外卖点单系统;界面下单支付;测试百度地图;自动贩卖机的人脸识别功能;预约界面;优惠券抢购界面
  - 用户登录页面哪些测试?

- 针对接口测试这个特定的情况,要清楚流程是什么,如何设计测试用例

  - 流程? 
    - 先理解接口文档,接口的业务功能,请求方法等以及对应的数据库存储.
    - 编写测试用例,有效等价类与无效等价类(接口的健壮性)
  - 用例注意什么? 
    - 理解接口功能,输入输出参数,数据格式,请求方法
    - 接口业务逻辑和实现的功能是否正常
    - 接口在正常请求与异常请求时的返回值是否正常,
    - 数据库校验,
    - 性能测试,
    - 安全性测试(敏感信息,权限控制)

- 白盒测试方法

  - 目标? 对程序模块的所有独立执行的路径至少测试一次.

  - 测试用例设计方法? 语句覆盖 < 分支覆盖 < 条件覆盖 < 路径覆盖,每一层都包含了上一层的要求，同时提出更严格的要求。

    ```java
    if (a > 0 && b > 0) {
        doX();
    }
    doY();
    ```

    **语句覆盖**只要求每行代码都执行一次。一个用例 a=1, b=1 就够了

    **分支覆盖**要求 if 的 true 和 false 两个分支都走到。加一个 a=-1, b=1 就行。但注意，这里 a>0 为 false 时直接短路了，b>0 根本没被求值，所以 b>0 为 false 的情况没测到。

    **条件覆盖**要求每个子条件（a>0 和 b>0）的 true/false 都取到。需要覆盖 a>0 为 true/false，b>0 为 true/false。这比分支覆盖更细，因为它拆开了复合条件里的每个原子条件。

    **路径覆盖**要求所有可能的执行路径都走到。这段代码有两条路径（进 if 和不进 if），看起来和分支覆盖差不多。但如果有两个串联的 if，分支覆盖只需要每个 if 的 true/false 各走一次，路径覆盖要求所有组合（true-true, true-false, false-true, false-false）都走到，用例数量会指数增长。

- 黑盒测试方法

  - 测试用例设计方法? 等价类划分,边界值分析,因果图,决策表,场景法,大纲法
  - 等价类划分? 
    - 是什么?把输入域划分成若干个等价类，从每个类中选一个代表值来测试
    - 分为两种：有效等价类（符合规格说明的合法输入）验证系统正确性，无效等价类（不符合规格说明的非法输入）验证系统健壮性。
    - 难点? 一是需要准确理解需求才能正确划分，需求理解偏了，等价类就会遗漏或不准确；二是当输入条件之间存在组合关系或相互依赖时，划分会变得复杂，单纯按单个字段划分可能不够，需要考虑条件之间的交互。
    - 有类型约束（编译器、框架自动校验）帮你挡住的，就不需要重复设计用例,比如向int方法参数类型传String,会被编译器拦住.
  - 边界值分析?
    - 大量缺陷发生在输入或输出范围的边界处，而不是中间值。所以边界值分析专门针对边界点设计测试用例。
    - 通常取的值包括? 正好在边界上的值（min、max）、刚好超出边界的值（min-1、max+1），有些严格的做法还会取边界内侧一步的值（min+1、max-1）,以及数据边界极端值,Integer.MAX_VALUE,先测业务边界,再测数据边界
    - 不是在某一个等价类内部随便选值，而是关注相邻等价类的交界处。因为bug最容易出现在"从一个等价类跨到另一个等价类"的那个临界点上。
  - 等价类划分与边界值分析在算法用例设计的例子?
    - 二分查找: 有效等价类: 长度为 0（空数组）、长度为 1、长度为 2、长度较大; 无效等价类:传入 null（而非空数组）、数组未排序
    - 空集合/空字符串、单元素/单字符、全部相同的元素、已排序和逆序、整数溢出（Integer.MAX_VALUE / MIN_VALUE）、链表/树的 null 节点、图中的环和孤立节点。
  - 决策表? 列出不同的输入组合以及对应输出,确保所有可能的组合都得到测试,适合处理多条件组合产生不同业务结果的场景
  - 场景法从用户使用系统的完整流程出发来设计测试用例。核心概念有两个：基本流（Basic Flow）和备选流（Alternative Flow）。场景法的优势在于它更贴近真实用户行为，适合验证端到端的业务流程，比如电商下单流程、ATM取款流程。

- 功能测试

  - 用例包含什么? 前置条件,测试步骤,测试数据,测试结果(预期与实际),通过/失败标准,测试环境,关联的bugID

- 性能测试

  - 性能测试是广义的,包含哪些具体测试? 基准测试(在正常预期负载),负载测试(逐步增加负载),稳定性测试(在一定负载下长时间执行),压力测试(负载加到超出系统正常承受能力,检查系统稳定性和恢复能力)
  - 怎么执行?
  - 指标? TPS,平均响应时间,并发数,错误率.
  - 压测的时候，QPS一直上不去，你会怎么排查？
    - QPS上不去本质上是遇到了瓶颈.排查链路: 压测端 → 网络 → 服务器资源 → 应用层 → 下游依赖 → 架构
      - 压测端:压测机自身性能问题或者压测脚本的问题
      - 网络: 网络带宽是否满了,或者延迟很高
      - 服务器资源:被测服务器上看四大指标: CPU top命令,内存 free -h,磁盘 I/O(日志写入量大),网络(服务端自身的带宽和连接数)
      - 应用层: 1.并发 线程池满了,tomcat/netty线程池小导致请求排队等待. 连接池太小,如数据库连接池,http连接池; 2. JVM问题 GC问题频繁full gc导致STW,导致QPS波动;代码锁竞争:把并发被串行化.
      - 下游依赖: 数据库出现慢sql,数据库连接池配置太小;redis大key,热key的问题;消息队列如kafka的消费速度是否跟得上;外部接口第三方服务响应慢.
        - 架构问题:单机到了极限,考虑水平扩容.读写分离.加缓存.

- 验收测试内容?

  - 包括α测试与β测试. α测试用户在受控的开发者环境进行;β测试:由软件最终用户在用户场所进行,开发者不在场,由用户记录遇到的问题再发给开发者

- **举一个集成测试的例子**

  - 系统调用controller->service->dao, 开发这三个模块完毕后,测试三个模块的集成,涉及到代码调用,关注模块之间的交互,数据传递.但是不关注模块的具体实现方式

- **在系统开发中,测试应该摆在什么位置?**

  - 如果先写测试,再进行开发,则是测试驱动开发TDD,而根据行为驱动开发则是BDD,这是测试与开发的交互方式.
  - 单元测试针对最小可测试部分进行,在开发完后进行,但如果是测试驱动,则是先开发单元测试代码,然后再写业务代码

- **为什么选择测试?我做测试会有什么优势?对测试的理解是什么?测试与开发的区别和联系在哪?为什么不去开发? 我的职业规划是什么?**

  - 选择测试的理由和我的优势 (https://claude.ai/share/aebc4e85-bd36-43af-b989-2d4b03b7aed4)
    - 业界理由: 软件测试为啥重要? AI下,软件测试为啥还重要?
      - 大语言模型的问题:幻觉,上下文限制,缺乏真正的理解,确定性和可复现性差. 跟Ai相比,测试工程师的不可替代性在于验证AI的结果是否可信
        - 幻觉问题: 设计穿透表面正确性的测试用例
        - 上下文限制: 跨模块的一致性丢失,测开站在系统全局视角,设计集成测试和端到端测试
        - 缺乏真正理解:测试工程师能把业务规则转化成可验证的测试场景,并且确定测试优先级,把业务风险映射到测试策略.
        - 确定性差: AI的输出本质上是非确定性的,AI修改代码后,测开建立回归测试以及上线前的测试检查,以保障代码质量的确定性
        - AI生成代码不会主动考虑安全维度问题,测开有自己的安全测试思维框架,主动去探测系统的薄弱点而不是等问题暴露.

    - 个人理由(优势):
      - 跨技术栈的工程能力契合测开的广度要求,项目技术栈广度高,具有跨语言和跨技术栈的工程能力.
      - AI应用能力，我在日常开发和学习中深度使用大模型，对它的能力和局限都有比较清晰的认知，知道怎么用它提效，也知道它的输出需要验证。这种素养放到测开工作中，可以比较快地把AI工具融入测试流程。
      - 批判性思维,擅长从不同角度思考问题
    
  - 对测试开发的理解:
    - 是系统质量保障,需要关注软件各层面的质量.是利用开发能力专注保障质量,利用开发能力可以发现系统潜在的问题以及寻找和判断bug.提前发现和修复缺陷以减少维护成本,提高用户满意度.
  
  - 测试与开发的区别和联系在哪? 为什么不去开发
    - 测开与开发的目标不同: 开发的目标是把需求变成可运行的功能,交付产品,而测开目标是保障产品质量，交付的是质量信心——让团队能确信系统是可靠的
    - 思维方式不同:开发是建设性思维,关注如何实现需求,系统如何设计,如何优化性能,而测开是批判性思维,考虑哪里会出错,什么场景没覆盖,系统的薄弱点. 开发是考虑怎么work,测开是怎么让他break
    - 技术侧重点不同: 开发通常在自己负责的模块上深耕，对某个技术栈钻得很深；测开则需要横跨多个模块，理解系统整体的交互方式
    - 我为什么选择测开:  我的思维偏向于批判性思维,比起实现功能本身,我更享受思考系统什么时候出现问题,记录和解决这些问题给我安全感.第二我喜欢测开这种需要广度的工作模式,跨领域能让我接触到不同的技术,喜欢这种了解全局的感觉.第三AI时代,代码生成容易但是质量保障很难,测开具有长期价值.
  
  - 职业规划?
    - 短期来说，扎根业务，打牢基础. 进入公司后我的首要目标是深入理解业务，把公司的技术栈和测试体系吃透，在实际项目中把自动化测试的工程能力打扎实，让自己先成为一个靠谱的执行者。
    - 中期, 建立深度，形成体系化能力. 在某一两个质量方向上建立深度，比如性能测试、安全测试、或者测试平台开发。同时开始具备独立设计整个项目测试方案的能力. 关注AI与测试的结合,比如用大模型辅助测试用例生成、智能缺陷分析这些方向，把AI工具融入测试体系中提升效率。
    - 我希望从执行者成长为质量体系的设计者，能够从全局视角推动团队的测试策略和基础设施建设。具体是走技术专家路线还是管理方向，我觉得可以根据实际情况来判断，核心是在质量工程这个领域持续深耕。
  
- **作为一个测试开发工程师，要参与哪些流程？**

  - 在需求评审阶段,关注需求是否可测试,减少模糊.多提出问题减少返工
  - 测试计划与用例设计,**评估哪些用例适合自动化、制定自动化策略**。
  - 参与开发的技术方案评审,从测试角度识别风险,推动开发提供合理日志.
  - 自动化用例编写,工具开发、框架建设、环境搭建这些属于**跨迭代的持续性工作**，有需要的时候才做
  - 测试执行= 跑自动化测试以及手工用例,发现 bug 后提交缺陷报告，跟踪修复，验证回归. 关注自动化用例的稳定性和效率.
  - 进行性能测试,安全测试,兼容测试等
  - 参与发布前的回归测试，确认核心功能没有被影响。线上发布后做冒烟验证，确认生产环境功能正常。如果有灰度发布，还要参与灰度验证。
  - 上线后: 参与线上问题的复现和定位，分析 bug 的根因，并做测试复盘——哪些 bug 是测试阶段遗漏的，为什么遗漏，怎么改进。

- **清楚软件测试的流程,每个环节做了什么,以及产出的文档**

  - 需求分析-Product Requirements Document 产出PRD,需求评审
  - 测试计划,描述测试过程的策略,范围,资源,时间表,目标;测试计划评审
  - 测试用例设计-基于测试计划设计,测试用例评审
  - 搭建测试环境,执行预测试(冒烟测试),正式测试,记录测试结果,报告缺陷---产出测试环境配置文档,测试脚本,测试执行日志以及错误报告.
    - 测试执行日志包含什么? 记录测试执行详细过程,包括测试用例执行结果
    - 错误报告包含什么? 缺陷描述,重现步骤,影响范围,严重程度
  - 回归测试,代码更改后执行,以确保新更改不破坏现有功能
  - 性能测试-评估系统响应时间,稳定性
  - 部署项目到预生产,进行预生产测试
  - 写测试报告,总结测试活动结果---产出测试报告
    - 测试报告包含什么? 报告总结测试的代码覆盖率,做了什么,发现了哪些缺陷,以及最终的测试评估.
  - 上线后,根据反馈复盘总结

- 知道bug生命周期

  - 发现->提交->指派->研发确认->修复->回归验证->关闭
    - 为什么需要指派? 因为bug来自哪个系统的哪个功能,由不同的开发负责,指派给对应的开发





# jmeter

http://www.51ste.com/share/det-897.html
https://leileiluoluo.com/posts/apache-jmeter-introduction.html

JMeter就像一群将请求发送到目标服务器的用户一样,收集来自目标服务器的响应以及其他统计数据,本质上是负载生成工具,不同类型的测试只是你在**线程数、Ramp-Up 时间、持续时间**这几个参数上的不同组合而已。

**基准测试**：线程组设置一个固定的、符合预期的用户数，跑一段时间，记录响应时间和吞吐量作为基线数据。

**负载测试**：利用线程组的 Ramp-Up 时间逐步增加并发用户数，或者使用阶梯式加压插件（Stepping Thread Group），观察不同负载下系统的表现拐点。

**压力测试**：把线程数设得远超系统预期承受能力，持续施压，观察系统什么时候开始报错、响应时间暴涨、甚至崩溃。

**稳定性测试**：设置一个中等负载，把持续时间拉得很长（比如几小时甚至一整天），配合监控工具观察内存、CPU 等资源是否随时间出现异常增长。

所以线程组参数很重要:

**Number of Threads (users): 1** 模拟的虚拟用户数。设为 1 就是 1 个用户，设为 100 就是 100 个并发用户。

**Ramp-up period (seconds): 1** 所有线程启动完毕所需的时间。比如 100 个线程、Ramp-up 设为 10 秒，那么每秒启动 10 个线程，逐步加压而不是瞬间全部涌入。

**Loop Count: 1 / Infinite** 每个线程执行测试逻辑的循环次数。设为 1 就跑一次就结束，勾上 Infinite 就无限循环（通常配合下面的 Duration 来控制什么时候停）。

**Same user on each iteration** 勾选后，每次循环迭代都使用同一个虚拟用户身份（比如同一个 cookie、session）。取消勾选的话，每次迭代相当于一个"新用户"，会重置状态。

**Delay Thread creation until needed** 默认情况下 JMeter 启动时会一次性创建所有线程对象。勾选后，线程会延迟到真正需要启动时才创建，可以在线程数很大时节省初始内存占用。

**Specify Thread lifetime** 勾选后激活下面两个时间控制选项。所以 Duration 和 Startup delay 变为可编辑状态。

**Duration (seconds):** 测试总共运行多长时间，单位秒。比如填 60 就是跑 1 分钟后自动停止。要把 Loop Count 勾为 Infinite 配合使用才有意义。

**Startup delay (seconds):** 点击运行后，延迟多少秒才真正开始执行这个线程组。适用于一个测试计划里有多个线程组、需要错开启动时间的场景。

## 了解jmeter 测试计划元件.

- 测试计划（Test Plan）
  - 通过jmeter gui创建,保存为xml格式

- 线程组（Thread Group）	
  - users
  - ramp-up period
  - loop count
- Logic Controller
  - 控制Sampler处理顺序,有循环,条件判断
- Sampler
  - 用于对被测试应用发起请求,每一个samper生成一组结果,采样器生成一个或多个采样结果；这些采样结果具有许多属性，例如经过时间、数据大小等在listener查看.
  - HTTP Request、FTP Request 和 JDBC Request
- Listener
  - 监听器用于监听、保存和读取测试结果，一般在测试流程的最后执行。默认情况下测试结果保存在一个后缀为`.jtl`的 XML 文件里。不同 Listener 的区别只在于**展示方式**
  - Graph Results 和 View Results in Table,View Result Tree
  - 集合报告,汇总报告
- Configuration Element
  - 设置变量和默认值以供后面的samper使用,先处理Configuration Element,再合并范围内的samper
  - HTTP Header Manager ,HTTP request default,HTTP Cookie Manager
- Assertion
  - 对samper结果进行额外检查,一般会添加到对应的采样器下。
- Timer
  - 控制采样的时间间隔,在请求之间暂停,无timer则请求立刻发出.
  - Constant Timer 会将每个线程在请求之前暂停相同的时间
  - 每个线程在发完一个请求后，会等待计时器指定的时间，然后再发下一个请求。这模拟的是真实用户的行为,每个线程之间计时器互相独立
- Pre Processor
  - 在采样执行前对请求进行配置,如HTTP URL重写修饰符
  - 常用的预处理器有 JSR223 PreProcessor 等。JSR223 PreProcessor 允许在采样前使用 JSR223 脚本代码做一些前置工作。

- Post Processor
  - Regular Expression Extractor 允许用户使用正则表达式从响应中提取信息；XPath Extractor 允许用户使用 XPath 查询语言从响应（XML 或 HTML）中提取信息。
  - 如果你希望JMeter在Web服务器显示错误时停止发送请求，那么你将使用后处理器执行此操作。
- test Fragment
  - test Fragment也是一种元件，例如“线程组”元件。唯一的区别是，除非模块控制器或包含控制器引用了测试片段，否则不会实现测试片段。
  - 模块控制器module controller：同文件内复用,而include controller是引用外部jmx文件


测试计划元件执行顺序: 配置元件(Configuration Element) -> 前置处理器 -> 计时器 -> 取样器 -> 后置处理器 -> 断言 -> 监听器

减少jmeter资源需求:

使用非gui执行测试;取代大量相似的采样器,而是在循环中使用相同的采样器，并使用变量来改变采样;不要在加载（压测）期间使用"查看结果树"或"查看表中的结果,这些监听器会把每一个请求的详细信息（请求头、响应体、响应时间等）都保存在内存里,占用大量内存,实际执行压力测试时应该删除或禁用.

使用`-l result.jtl` 把压测结果保存到文件,用jtl文件进行分析.(**J**Meter **T**est **L**og)



jmeter与性能测试有什么关系?

JMeter提供性能测试方面的优势，例如：

- 它可以用于测试静态资源和动态资源的性能；
- 它可用于测试网站最大并发用户数，从而分析定位网站瓶颈；
- 它提供了性能报告的图形化分析；


## 参数化

## 提取动态数据

json path表达式

JMeter中每个线程（虚拟用户）的变量是**互相隔离**的。线程1登录后提取的 `${token}` 只属于线程1，线程2提取的 `${token}` 只属于线程2，互不干扰。

## 断言

## 查看和分析结果

聚合报告指标

- **Average**：所有请求的平均响应时间，单位毫秒。最常看的指标，但容易被极端值拉高，所以不能只看这一个。
- **Median**：中位数，50%的请求在这个时间内完成。比平均值更能反映大多数用户的真实体验。
- **90% Line**：90%的请求在这个时间内完成。比如值是500ms，说明只有10%的请求超过500ms。
- **95% Line**：95%的请求在这个时间内完成。很多公司用这个作为性能达标的标准，比如要求"95%的请求响应时间在1秒以内"。
- **99% Line**：99%的请求在这个时间内完成。关注极端情况下最慢的那1%用户的体验。
- **Min**：最快的一次请求耗时。
- **Max**：最慢的一次请求耗时。如果Max远大于Average，说明存在偶发的性能抖动。
- Error %：错误率，请求失败的比例。正常压测中这个值应该很低，如果突然升高说明系统在当前负载下已经扛不住了。
- **Throughput**：吞吐量，每秒处理的请求数（requests/sec）。反映系统的处理能力，是性能测试中最核心的指标之一。吞吐量越高说明系统处理能力越强。
- **Received KB/sec**：每秒接收的数据量。用来观察网络带宽是否成为瓶颈。
- **Sent KB/sec**：每秒发送的数据量。同上，关注上行带宽。

重点关注 average,95%, error,throughput





# Pytest

https://realpython.com/pytest-python-testing/

https://learning-pytest.readthedocs.io/zh/latest/

官方文档 https://docs.pytest.org/en/stable/getting-started.html

## 核心概念并不多:

## pytest命令行

`pytest --durations=5` 报告中显示前5个耗时最长的测试用例.

执行测试时,指定某些标记的函数执行 `pytest -m "finished and commit"``pytest -m "finished and not merged"`这样的逻辑表达式,指定执行或者不执行某些方法.

`-k EXPRESSION` 按名称过滤，匹配测试函数名、类名、模块名。比如 `-k "login or register"` 会运行名字里包含 login 或 register 的用例。和 `-m` 的区别在于 `-k` 是基于名字的字符串匹配，`-m` 是基于显式标记。

`-v` 详细模式

`-s` 禁用输出捕获，让 print 的内容直接显示在终端

`-x` 遇到第一个失败就停止整个测试运行

`--maxfail=N` 失败达到 N 个就停止，比 `-x` 灵活一点。

`--alluredir=path` 配合 allure-pytest 插件，生成 Allure 报告数据

## Fixture

https://docs.pytest.org/en/stable/how-to/fixtures.html

固件fixture,是一些函数,pytest会在执行测试函数前或者之后加载运行. 最常见的是数据库初始链接和最后关闭.

使用文件 `conftest.py` 集中管理固件,在不同的目录层级定义 `conftest.py`，其作用域为其所在的目录和子目录。把 conftest 当做插件来理解,会自动调用.

```python
@pytest.fixture()
def postcode():
    return '010'

  
def test_postcode(postcode): # 作为函数参数自动传递,postcode是fixture的返回值.	
    assert postcode == '010' 
  
@pytest.fixture()
def db():
    print('Connection successful')

    yield # 用yield来将固件分为两部分,为预处理和后处理.

    print('Connection closed')
```

fixture有scope,精细控制使用范围.

- function 默认层级
- class 每个测试类执行一次
- module 
- session 一次测试执行一次,所有被找到的函数和方法都可用

fixture不会自动调用,手动调用时,用`@pytest.mark.usefixtures` 标注,或者引用fixture的函数名作为参数. 

可以设置 fixture自动调用 `@pytest.fixture(scope='session', autouse=True)`

固件名称默认函数名,用name标签改变名称 `@pytest.fixture(name='age')`

相比于函数参数化,固件参数化用到params标签.

```python 
@pytest.fixture(params=[
    ('redis', '6379'),
    ('elasticsearch', '9200')
])
def param(request): # 用到内置固件request 和 param参数,获取对应的参数
    return request.param
  
  
@pytest.fixture(scope="module", params=["smtp.gmail.com", "mail.python.org"])
def smtp_connection(request):
    smtp_connection = smtplib.SMTP(request.param, 587, timeout=5)
    yield smtp_connection
    print(f"finalizing {smtp_connection}")
    smtp_connection.close()
```



一些内置固件,提一下 `tmpdir & tmpdir_factory`

tmpdir: 使用 `tmpdir.mkdir()` 创建目临时录，`tmpdir.join()` 创建临时文件（或者使用创建的目录）。只能在function 作用域

`tmpdir_factory` 可以在所有作用域使用.

### conftest.py

## mark

https://docs.pytest.org/en/stable/how-to/mark.html

mark是做标记,标记一个函数的性质. 一个函数可以有多个标记,不同函数之间的标记不冲突.



标记函数应该被跳过. `@pytest.mark.skip(reason='out-of-date api')` ,也可以指定条件跳过

```python
@pytest.mark.skipif(conn.__version__ < '0.2.0',
                    reason='not supported until v0.2.0')
```

**`xfail`** 表示预期测试会失败，因此如果该测试*失败* ，整个测试套件仍可能获得通过。

## parametrize

https://docs.pytest.org/en/stable/how-to/parametrize.html

参数化测试，即每组参数都独立执行一次测试,`pytest.mark.parametrize(argnames, argvalues)`

```python
@pytest.mark.parametrize('user, passwd',
                         [('jack', 'abcdefgh'),
                          ('tom', 'a123456a')])
def test_passwd_md5(user, passwd):
```

对于argvalues,是一个列表, 列表项可以是`pytest.param('jack', 'abcdefgh', id='User<Jack>'`,也可以是元组,包含对应的参数列表.

## allure集成

安装pytest-allure,指定pytest的allure报告输出目录.

在具体的代码中使用allure的装饰器或者语句. 每一个装饰器都有对应的dynamic写法. 常用的如下:

- @allure.epic("项目名")

- @allure.feature("模块名") 对应功能模块

- @allure.story("场景名") 对应模块下的具体用户场景

- `@allure.title("用正确的用户名密码登录成功")` 设置用例标题，替代报告里默认显示的函数名,支持占位符,@allure.title("使用 {username} 登录")

- @allure.severity(allure.severity_level.CRITICAL)

  - 分为五个等级,BLOCKER（阻塞）、CRITICAL（严重）、NORMAL（普通，默认值）、MINOR（次要）、TRIVIAL（轻微）。

- @allure.step("输入用户名: {username}") step是动态的,没有dynamic,可以用装饰器形式和 with 形式. 如果方法会在很多用例里反复调用则用装饰器形式,如果是用例特有的业务流程，不需要复用，用 with 在用例里直接组织逻辑就行

- attach,attach 也没有 dynamic 版本,往报告里添加附件

  ```python
  allure.attach(
      body=json.dumps(response_data, ensure_ascii=False, indent=2),
      name="响应数据",
      attachment_type=allure.attachment_type.JSON
  )
  ```

allure报告的内容

# Jenkins



https://claude.ai/share/b1a04db6-7df2-4bf5-8612-f9cfaccd4cf8

JMeter可以集成到Jenkins,设置为定时任务,检测任务的错误率和响应时间,装 Performance 插件:

```groovy
perfReport sourceDataFiles: 'result.jtl',
           errorFailedThreshold: 1,
           errorUnstableThreshold: 0.5
```

jenkins集成allure报告,装 Allure 插件,设置Allure Commandline为allure.不管测试通过还是失败，都要生成报告，所以用`post { always {} }` `post` 就是 Pipeline 所有 Stage 跑完之后执行的收尾动作。

```groovy
pipeline {
    agent any

    stages {
        stage('跑测试') {
            steps {
                sh 'pytest --alluredir=allure-results'
            }
        }
    }

    post {
        always {
            allure includeProperties: false,
                   results: [[path: 'allure-results']]
        }
    }
}
```

groovy用法

```groovy
pipeline {
    agent any              // 在哪跑，any 表示任意节点
    environment {          // 环境变量
        SERVER_IP = '1.2.3.4'
    }
    stages {
        stage('阶段名') {
            when { branch 'main' }  // 条件控制，只在特定分支执行
            steps {
                sh '执行shell命令'      // 核心：跑命令
                echo '打印信息'
                dir('/path') { ... }   // 切换目录执行
                git url: '...', branch: 'main'  // 拉代码
            }
        }
    }
    post {
        always  { /* 无论成败都执行，如生成报告 */ }
        success { /* 成功时执行，如部署、通知 */ }
        failure { /* 失败时执行，如告警 */ }
    }
}
```

jenkins凭证管理

```groovy
// 用户名密码（如 DockerHub）
withCredentials([usernamePassword(
    credentialsId: 'dockerhub-creds',
    usernameVariable: 'USER',
    passwordVariable: 'PASS'
)]) {
    sh 'echo $PASS | docker login -u $USER --password-stdin'
}

// SSH 密钥（如部署到服务器）
sshagent(['server-ssh-key']) {
    sh "ssh root@${SERVER_IP} 'docker pull myapp && docker restart myapp'"
}

// 单个密钥字符串（如 API Key）
withCredentials([string(credentialsId: 'openai-key', variable: 'OPENAI_API_KEY')]) {
    sh 'docker run -e OPENAI_API_KEY=$OPENAI_API_KEY myapp:latest'
}
```



Jenkins 任务用Groovy语言写出, 在我的项目中,流程是: 

1. 推送后端代码到github,一般是dev分支.
2. 触发jenkins任务构建
3. 任务拉取代码,构建镜像,并且部署到测试环境,现在是部署到我的环境
4. 跑测试,把测试框架打包成镜像运行,测试通过才能执行下一步.所以测试框架里不要写死地址.
5. 部署到生产环境,用 SSH 部署

对于后端中的敏感信息api-key,通过读取环境变量,jenkins通过凭证注入环境变量.



自动化测试框架
=======

自动化测试框架https://www.yuque.com/chengxuyuancarl/fzian3/abkg8txkrtogh86t



首先获取接口文档,然后进行测试用例的设计,针对单接口和业务逻辑接口

单接口测试: 正向测试,反向测试,边界测试

业务逻辑测试:存在依赖关系的接口,测试接口之间的协作关系, 需要将上一个接口的内容作为下一个接口的输入.

postman和jmeter可以快速验证接口功能,自动化测试是把手动测试脚本化.



测试驱动开发先编写测试用例,再用最小的代码通过测试.保证代码的每个部分都是可以通过测试的.

行为驱动测试,开发前应该由各方人员定义,用自然语言编写测试用例



测试自动化框架实现方式不重要,要围绕解决什么问题,关注项目的实用价值以及对业务需求的理解.



框架的目标,

框架的设计思路,

如何落地到团队流程,加入到CI

工具的使用以及常见问题的排查



项目这块可以列一些量化数据，比如提效了xx%，节省了多少人力/人天；自动化测试解决了什么痛点，收益是什么

不要单纯列你干了什么事，而且带着目的想过程：你为了解决什么问题才做了这个事

比如：用例通过率不是重点，重点是稳定率有没有提高，维护成本降低了没有，想一想写自动化用例的目的，为什么要做这个事

简历缺乏自己的思考,okr的视角，你做的每一件事目的是什么。然后先写背景和目标，再写行为，然后再写收益







